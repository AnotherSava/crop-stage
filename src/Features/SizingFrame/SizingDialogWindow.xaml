<Window x:Class="CropStage.Features.SizingFrame.SizingDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sizing Frame"
        WindowStyle="None"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="Segoe UI"
        FontSize="13"
        Background="#FAFAFA"
        Cursor="SizeAll">
    <Window.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="8,0,8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SpellCheck.IsEnabled" Value="False"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="BorderBrush" Value="#CFCFCF"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="{x:Type TextBlock}">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,14,0"/>
            <Setter Property="Foreground" Value="#444"/>
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="MinWidth" Value="32"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#CFCFCF"/>
            <Setter Property="Cursor" Value="Arrow"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="BorderBrush" Value="#0067B8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#B0B0B0"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#A0A0A0"/>
                                <Setter Property="Foreground" Value="#ECECEC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GhostButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E8E8E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D8D8D8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border BorderBrush="#C8C8C8" BorderThickness="1">
        <Grid x:Name="RootGrid" Margin="14,6,14,6" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Size -->
            <TextBlock x:Name="SizeLabel" Grid.Row="0" Grid.Column="0" Text="Size" Style="{StaticResource FieldLabel}"/>
            <StackPanel x:Name="SizeFields" Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                <TextBox x:Name="WidthBox" Width="80"/>
                <TextBlock Text="×" VerticalAlignment="Center" Width="12" Margin="10,0" FontSize="15" Foreground="#888"/>
                <TextBox x:Name="HeightBox" Width="80"/>
            </StackPanel>
            <Button Grid.Row="0" Grid.Column="2"
                    x:Name="ExpandedToggleButton"
                    Content="&#xE70E;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="11"
                    Width="32" Height="28"
                    Margin="8,4,0,4"
                    Style="{StaticResource GhostButton}"
                    ToolTip="Compact mode"/>

            <!-- Folder -->
            <TextBlock x:Name="FolderLabel" Grid.Row="1" Grid.Column="0" Text="Folder" Style="{StaticResource FieldLabel}"/>
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="FolderBox" Width="192"/>
            <Button Grid.Row="1" Grid.Column="2" x:Name="BrowseButton" Content="…" Width="32" Height="28" Margin="8,4,0,4" FontSize="14"/>

            <!-- Filename -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Filename" Style="{StaticResource FieldLabel}"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="FilenameBox" Width="192"/>
            <Button Grid.Row="2" Grid.Column="2"
                    x:Name="ScreenshotButton"
                    Content="&#xE114;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="13"
                    Width="32" Height="28"
                    Padding="0"
                    Margin="8,4,0,4"
                    Style="{StaticResource PrimaryButton}"
                    ToolTip="Screenshot"/>
            <Button Grid.Row="2" Grid.Column="3"
                    x:Name="CompactToggleButton"
                    Content="&#xE70D;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="11"
                    Width="32" Height="28"
                    Margin="6,4,0,4"
                    Style="{StaticResource GhostButton}"
                    ToolTip="Expanded mode"
                    Visibility="Collapsed"/>
        </Grid>
    </Border>
</Window>
